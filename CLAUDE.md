# Claude — see [`AGENTS.md`](./AGENTS.md)

This repository follows the unified PerfOpsLab agent guidance documented in
[`AGENTS.md`](./AGENTS.md). All hard rules, reusable workflows, VPS deploy
contract, and conventions apply equally to Claude Code, Claude API agents,
and any other LLM-based tooling.

Repo-specific notes (when present) live in the repo's own `CLAUDE.md`. They
override the org defaults.
